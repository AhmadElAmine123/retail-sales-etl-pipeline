# Database Configuration
POSTGRES_DB=retail_db
POSTGRES_USER=retail_user
POSTGRES_PASSWORD=your_secure_password_here

# Airflow Configuration
AIRFLOW_DB=airflow
AIRFLOW_USER=airflow
AIRFLOW_PASSWORD=your_airflow_db_password_here
AIRFLOW_USERNAME=admin
AIRFLOW_ADMIN_PASSWORD=your_admin_password_here
AIRFLOW_UID=50000

# Optional: Fernet key for encrypting connections (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
AIRFLOW_FERNET_KEY=

# Optional: Additional pip requirements
_PIP_ADDITIONAL_REQUIREMENTS=pandas>=2.0.0 psycopg2-binary>=2.9.0
